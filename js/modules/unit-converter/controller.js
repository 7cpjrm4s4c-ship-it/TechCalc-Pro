export default {
  // Unit converter has no module-specific controller hooks at RC time.
  // The file exists to keep the module contract aligned with the platform
  // reference architecture and to make future actions explicit.
};
