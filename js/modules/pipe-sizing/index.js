import config from './config.js'; import { state } from './state.js'; import { calculate } from './logic.js';
import { card, field, selectField, resultRows, renderModuleShell, bindCommonInputs } from '../../core/renderer.js'; import { fmt } from '../../utils/calculations.js'; import { pipeSystems } from '../../utils/pipes.js';
function view(s){ const r=calculate(s); return renderModuleShell(config, `<div class="span-12">${card('Basisdaten', `<div class="form-grid">${selectField({id:'systemId',label:'Rohrsystem',value:s.systemId,options:pipeSystems.map(p=>({value:p.id,label:p.label}))})}${field({id:'maxPressurePam',label:'Max. Druckverlust',unit:'Pa/m',value:s.maxPressurePam})}${field({id:'massFlowKgh',label:'Massenstrom',unit:'kg/h',value:s.massFlowKgh})}${field({id:'volumeFlowM3h',label:'Volumenstrom',unit:'m³/h',value:s.volumeFlowM3h})}</div>`,'blue')}${card(`Ergebnis — ${r?.system?.label ?? 'Rohrsystem'}`, r ? resultRows([{label:'Empfohlene DN',value:'DN '+r.dn},{label:'Geschwindigkeit',value:fmt(r.velocity),unit:'m/s'},{label:'Druckverlust',value:fmt(r.pressureLoss),unit:'Pa/m'},{label:'Norm',value:r.norm}]) : '<div class="empty-state">Volumenstrom oder Massenstrom eingeben →</div>','blue')}<div class="formula">≤ DN50: DIN EN 10255 · ≥ DN65: DIN EN 10220 · Auslegung nach Druckverlustgrenze</div></div>`)}
export default { config, state, mount(root){ const render=()=>{ root.innerHTML=view(state.get()); bindCommonInputs(root,state,render); }; render(); } };
