# Phase 16 Summary

Generated by Phase 37B.4 Documentation Consolidation. This file replaces 6 historical phase notes for Phase 16.

## Consolidated Changes

### Phase  16A ROUTER LIFECYCLE

Phase 16A verschiebt den eigentlichen Modulwechsel in eine zentrale Runtime. Der Router entscheidet nur noch, welches Modul aktiv werden soll. Die Runtime führt den verbindlichen Lifecycle aus: 1. `beforeUnmount` 2. `unmount` 3. `prepareMount` 4. `mount` 5. `afterMount`

Key sections: Zielzustand; Übergangsregel.

### Phase  16B SAVED RECORD CONTROLLER

Phase 16B starts the extraction of save/update/select/delete/accordion workflows from module code into a central controller. Modules should not own saved-record interaction behavior. A module may still define the domain snapshot/hydration functions, but the platform owns:

Key sections: New central contract; Migration rule.

### Phase  16C SCHEMA RENDERER

Phase 16C introduces the central schema renderer as the next platform layer after the router/lifecycle and saved-record controller. Modules should no longer own input markup, result cards, select attributes, segment buttons, or field-level event wiring. A module should provide:

Key sections: Goal; New central file; Compatibility; Contract; Why this matters.

### Phase  16D CSS SYSTEM

Phase 16D starts the move from module-owned visual rules to a platform-owned CSS system. New and migrated modules must compose global `tc-*` primitives instead of introducing module-specific UI classes. Modules may keep legacy selectors temporarily, but every new migration step must reduce module UI classes such as:

Key sections: Goal; Added platform primitives; Migration rule; Quality gate; Next step.

### Phase  16E 1 RAINWATER RECOVERY

Regenwasser nach der Event-Pipeline-Konsolidierung wieder mountbar machen. Das Regenwasser-Modul registriert nach Phase 16E einen zentralen Pipeline-Commit-Hook fuer Lookup-Hydration, importierte `registerPipelineCommitHandler` jedoch nicht aus `eventPipeline.js`. Dadurch wurde der Fehler erst beim Modul-Mount sichtbar: Navigation und Router funktionierten, aber `bindActions()` warf eine Runtime-Exception und der zen

Key sections: Ziel; Ursache; Fix; Regel.

### Phase  16E EVENT PIPELINE

Die Plattform übernimmt weitere UI-/UX-Ereignisse zentral. Module sollen nicht mehr eigene DOM-Events für Standardaktionen wie Speichern, Aktualisieren, Auswahl, Löschen, Accordion oder Lookup-Hydration binden. Der direkte Modul-Event-Score wurde weiter reduziert. Verbleibende Event-Altlasten liegen noch in legacy-lastigen Modulen wie Trinkwasser, WRG/Mischluft, h,x und Schmutzwasser und werden in den nächsten Entker

Key sections: Ziel; Umgesetzt; Ergebnis.



## Saved Record Mapping Contract

- Phase 16B definiert den zentralen Saved-Record-Workflow und die Domain-Mapping-Funktion `snapshot(current, result)`.

## Source Notes

Original files were intentionally consolidated in Phase 37B.4. The canonical history is now this file plus Git history and RELEASE_NOTES.md.
