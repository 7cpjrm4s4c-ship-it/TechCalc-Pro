# Phase 37 Summary

Generated by Phase 37B.4 Documentation Consolidation. This file replaces 10 historical phase notes for Phase 37.

## Consolidated Changes

### Phase  37a platform convergence audit

Ziel: alle Module gegen die Referenzarchitektur Heizung, Lüftung, Druckhaltung und Pufferspeicher prüfen. Der Audit verändert keine Runtime-Logik; er erzeugt eine belastbare Cleanup-Backlog-Basis für Phase 37B/37C. \* Referenzmodul. 1. **P2 · buffer-storage · css-specialization** — Modulspezifische CSS-Selektoren gefunden; gegen globale Tokens/Komponenten prüfen.

Key sections: Executive Summary; Modul-Scorecard; Wichtigste Cleanup-Felder; CSS-Gate; Utility-Kandidaten für Phase 37B.

### Phase  37a platform convergence closure

**Phase 37A abgeschlossen.** Phase 37A wurde als Stabilisierungs- und Konvergenzphase geführt. Ziel war kein Feature-Ausbau, sondern die belastbare Bewertung und Bereinigung der Plattformabweichungen nach Abschluss von Phase 36. Die verbleibenden 6 P2-Findings sind bewusst nicht mehr Teil von 37A, weil sie keine aktuelle Runtime-Instabilität erzeugen:

Key sections: Status; Ergebnis; Abgeschlossene Teilphasen; 37A – Audit Baseline; 37A.1 – P1 Convergence Cleanup.

### Phase  37a1 p1 convergence cleanup

Ziel: die in Phase 37A identifizierten P1-Findings schließen, ohne Runtime-Verhalten fachlich zu verändern und ohne Feature-Arbeit zu starten. 37A.1 ist bewusst klein gehalten. Die Phase schließt die harten Konvergenzrisiken, ohne neue Architektur einzuführen. Die verbleibenden Punkte sind P2: Runtime-Metadaten, CSS-Spezialisierungen, Event-Dichte und Utility-Kandidaten. Damit ist die Plattform-Konvergenz wieder unte

Key sections: Änderungen; Rainwater · Event-Boundary; Unit Converter · Module Contract; Audit-Ergebnis nach 37A.1; Validierung.

### Phase  37a2 runtime metadata cleanup

Phase 37A.2 bereinigt die im Platform-Convergence-Audit verbliebenen `runtime-metadata`-Findings. Die historischen `migrationStatus`-Breadcrumbs wurden aus allen Modul-Configs entfernt, damit Produktions-Runtime-Objekte keine Migrationshistorie mehr tragen. Die Bereinigung ist bewusst risikoarm: keine Fachlogik, keine Renderer, keine Event-Pipeline und keine Persistenzpfade wurden geändert. Die Migrationshistorie ble

Key sections: Ziel; Änderungen; Ergebnis; Bewertung.

### Phase  37b browser runtime smoke

Phase 37B ergänzt die bisherige Node-Testbasis um eine Browser-Runtime-Smoke-Schicht. Der Fokus liegt auf den Risiken, die Node-Tests strukturell nicht abdecken: echtes Routing im Browser, DOM-Mounts, mobile Pointer-Gesten, Scroll-Lock-Verhalten und Service-Worker-Offline-Fähigkeit.

Key sections: Ziel; Umfang; Neue Artefakte; Ergebnis; Ausführung.

### Phase  37b1 browser runtime execution

Phase 37B.1 überführt den in Phase 37B angelegten Browser-Runtime-Smoke von einer reinen Harness-Baseline in eine ausführbare Runtime-Prüfung. Der Runtime-Smoke bleibt der Playwright-Spezifikation `tests/e2e/phase37b-runtime-smoke.spec.mjs` zugeordnet. Phase 37B.1 stellt sicher, dass die App ohne externes `http-server`-Paket über einen lokalen statischen Server gestartet werden kann.

Key sections: Ziel; Anpassungen; Ausführungslogik; Validierung in dieser Umgebung; Runtime-Logik.

### Phase  37b1a browser console cleanup

Phase 37B.1A schliesst die ersten realen Browser-Runtime-Befunde aus der Netlify/Edge-Konsole nach Phase 37B.1. Zielzustand: Die Browser-Konsole zeigte reproduzierbar: Die Fehler betrafen insbesondere: `js/core/renderer.js` definiert jetzt eine lokale `cssEscape()` Utility.

Key sections: Ziel; Befund; Umsetzung; 1. Renderer cssEscape Guard; 2. safeReplaceContent Reentrancy Guard.

### Phase  37b1b browser runtime error elimination

Die nach 37B.1A im Browser sichtbaren Console-Fehler werden gezielt geschlossen: `eventPipeline.js` nutzt jetzt den expliziten Import `preserveFocusDuring` aus `focusManager.js`. Damit ist die zentrale Keyboard-Refresh-Strecke nicht mehr von einer nicht vorhandenen globalen `PlatformFocusManager`-Referenz abhängig.

Key sections: Ziel; Änderungen; Event Pipeline; Trinkwasser Dynamic Renderer; Erwartung im Browser.

### Phase  37b2 browser runtime smoke expansion

Phase 37B.2 erweitert die in 37B eingefuehrte Browser-Runtime-Abdeckung. Nach der Console-Bereinigung aus 37B.1A/37B.1B liegt der Fokus nicht mehr auf einzelnen Runtime-Fehlern, sondern auf reproduzierbarer Browser-Verhaltensabsicherung. Erweitert wurde `tests/e2e/phase37b-runtime-smoke.spec.mjs` um sieben Szenariogruppen:

Key sections: Ziel; Umfang; Technische Leitentscheidung; Dateien; Ergebnis.

### Phase  37b3 service worker offline hardening

Phase 37B.3 haertet die PWA-/Offline-Schicht. Nach 37B.2 war ein Offline-Reload-Smoke vorhanden, aber der Service-Worker-Precache enthielt nicht den vollstaendigen Runtime-Surface der ungebuendelten ES-Module. Fuer eine Cache-First-PWA ohne Bundler muss der Install-Cache alle produktiven JS-Module, CSS-Dateien, Shell-Dateien und Icons enthalten.

Key sections: Ziel; Umsetzung; Erweiterte Browser-Smoke-Abdeckung; Neuer Guard; Ergebnis.


## Source Notes

Original files were intentionally consolidated in Phase 37B.4. The canonical history is now this file plus Git history and RELEASE_NOTES.md.

### Phase 37B4 Documentation Consolidation

Phase 37B.4 consolidated historical phase notes into one summary file per phase. The phase documentation surface was reduced from 145 markdown files to 25 markdown files, including this README. Runtime code was not changed.

Validation target: fewer files, smaller documentation payload, preserved phase-level traceability, and updated tests that reference consolidated phase documents.

## Phase 37A Closure Compatibility Notes

- Phase 37A abgeschlossen
- P1-Findings: 0
- P2-Restpunkte: 6

## Phase 37C App-Shell Decomposition Closure

Phase 37C reduced `js/core/app.js` from the original monolithic shell toward a Composition Root. Theme, Settings, Release Notes, Feedback and Service Worker responsibilities were extracted to `js/platform/shell/*Controller.js`. Phase 37C.7 added the final closure audit and guard for controller presence, app.js size, extracted responsibility boundaries and service-worker precache coverage.

Validation target: `test:phase37c7`, `build`, `test:phase37c6`, `test:phase37c5`, `test:phase37c4f`, `test:module-smoke`.
