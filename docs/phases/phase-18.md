# Phase 18 Summary

Generated by Phase 37B.4 Documentation Consolidation. This file replaces 11 historical phase notes for Phase 18.

## Consolidated Changes

### Phase  18B1 HEATING COOLING RESULT MODEL

Basis: Phase 17C.17. Ziel dieser Phase ist die erste Entkopplung des Moduls Heizung/Kälte von eigener Ergebnisdarstellung, ohne den stabilen Modul-Mount bereits zu ersetzen. Diese Bereiche werden in folgenden 18B-Phasen migriert, damit das stabile Verhalten aus Heizung/Kälte nicht durch einen zu großen Umbau gefährdet wird.

Key sections: Umsetzung; Bewusst beibehalten; Ergebnis; Regression.

### Phase  18B2 HEATING COOLING LINE SECTION CONTROLLER

Heizung/Kälte bleibt während der Migration stabil, verliert aber weitere Modul-eigene Plattformlogik. Die Leitungsabschnitt-Verwaltung wird aus dem Modul heraus in einen zentralen Plattform-Controller verschoben. Neu: js/platform/lineSectionController/index.js Der Controller übernimmt generisch:

Key sections: Ziel; Umsetzung; Ergebnis; Regression.

### Phase  18B3 HEATING COOLING DYNAMIC RENDERER

Die Dynamic-Island-Mechanik des Moduls Heizung/Kälte wurde aus dem Modul in eine zentrale Plattformkomponente verschoben. Heizung/Kälte enthält weiterhin die fachliche Zustandsableitung, aber nicht mehr die eigene Dynamic-Renderer-Infrastruktur.

Key sections: Ziel; Umsetzung; Ergebnis.

### Phase  18B4 HEATING COOLING PLATFORM MOUNT

Move the Heizung/Kälte lifecycle onto the platform runtime without changing the stable UI behavior proven in Phase 18B.3. The current Heizung/Kälte UI still contains mode-dependent input islands and unit-specific fields that were proven stable in the existing view. Phase 18B.4 therefore moves the lifecycle first while preserving the proven view. The final schema-only view can follow after additional schema capabiliti

Key sections: Goal; Changes; Why not use the generic schema view yet?; Regression.

### Phase  18B4A HEATING COOLING ORDERED LAYOUT

Ziel dieser Phase ist die Wiederherstellung der fachlich festgelegten Heizung/Kälte UI-Reihenfolge nach der Plattform-Mount-Migration. Die Plattform darf die Cards nicht generisch als `System`, `Heizung`, `Kälte`, `Saved Records` sortieren. Heizung/Kälte folgt weiterhin der bewährten fachlichen Reihenfolge:

### Phase  18C PLATFORM HARDENING

Basis: Phase 18B.4A mit wiederhergestellter Heizung/Kälte-UI-Reihenfolge. Nach Regenwasser, Schmutzwasser und Heizung/Kälte sind drei fachlich unterschiedliche Module auf der Plattformstruktur stabil. Phase 18C haertet den Vertrag, entfernt reine Quelltext-Kompatibilitaetsmarker und aktualisiert alte Regressionen, sodass sie die neue Plattformverantwortung pruefen statt alte Modul-Implementierungen zu erzwingen.

Key sections: Ziel; Umgesetzt; Plattformvertrag nach 18C; Nicht Bestandteil.

### Phase  18C1 PIPE ISLAND GUARD

Changing the pipe material in Heizung/Kälte must only refresh the pipe recommendation island. The global navigation pill and the app shell must remain mounted. `pipeSystemId` is treated as a recommendation-only update in `platform/dynamicRenderer`: `tests/heating-cooling-phase18c1-pipe-island-guard.test.mjs` verifies that a pipe-only change updates the recommendation island without touching a simulated nav node or un

Key sections: Goal; Platform rule; Regression.

### Phase  18C2 SELECT FOCUS NAV GUARD

Nach Auswahl des Rohrwerkstoffs in Heizung/Kälte verschwand die Mobile Nav Pill. Die Navigation war nicht durch den Pipe-Recommendation-Renderer entfernt, sondern wurde durch die globale CSS-Regel `body.tc-keyboard-open .module-nav` ausgeblendet. `select[data-field]` war im Keyboard-Focus-Selector enthalten. Native Select-Picker auf iOS öffnen jedoch keine Software-Tastatur. Beim pipeSystemId-Select blieb `tc-keyboar

Key sections: Problem; Ursache; Fix; Ergebnis.

### Phase  18D HEATING COOLING REFERENCE CONSOLIDATION

Phase 18D konsolidiert Heizung/Kälte nach der erfolgreichen Plattform-Migration. Der Fokus liegt nicht auf einer neuen UI-Struktur, sondern auf der Entkernung verbliebener Modul-Altlasten. `modules/heating-cooling/index.js` nach `modules/heating-cooling/controller.js`

Key sections: Umsetzung; Zielzustand.

### Phase  18D1 HEATING COOLING REFERENCE CLEANUP

Heizung/Kälte bleibt auf dem stabilen Plattform-Mount aus Phase 18D, aber die verbliebene View-/Dynamic-Callback-Komposition wird aus dem Modul-Index herausgelöst. Heizung/Kälte folgt damit weiter dem Plattformvertrag: Mount, Dynamic-Islands, Result-Rendering und Leitungsabschnitte werden zentral gesteuert; fachliche Zustandsableitung bleibt im Modul-Controller.

Key sections: Ziel; Änderungen; Architekturstand.

### Phase  18D2 HEATING COOLING VIEWMODEL SPLIT

Heizung/Kälte now separates controller/state derivation from field composition.

Key sections: Changed; Contract.


## Source Notes

Original files were intentionally consolidated in Phase 37B.4. The canonical history is now this file plus Git history and RELEASE_NOTES.md.
